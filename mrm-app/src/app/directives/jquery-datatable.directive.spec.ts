import { JqueryDatatableDirective } from './jquery-datatable.directive';

describe('JqueryDatatableDirective', () => {
  it('should create an instance', () => {
    const directive = new JqueryDatatableDirective();
    expect(directive).toBeTruthy();
  });
});
